import { Stack, styled } from "@mui/material";

export const UpdateUserFormWrapper = styled(Stack)(() => {
    return {
        gap: "var(--flex-gap)",
        padding: "0 var(--basic-padding) var(--basic-padding)",
        overflow: "hidden",
    }
})